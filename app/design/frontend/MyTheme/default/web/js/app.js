define(['jquery','domReady!'], function($, dmReady){
	define([ "jquery", "crausel.slick"], function($, carosal) {
		 define([
			  "jquery",
			  "jquery.ui",
			  "jquery.fancybox",
			  "jquery.scrollme",
			  "crausel.slick",
			  "jquery.matchHeight",
			  "knoke.load"
			], 
			function($, $ui, $fanc, $scrl, carosal, $matchHi, $knoke) {
			  "use strict";
				$(document).ready(function($){
					console.log("Application successfully integrated.");
				});
			});
	});
	
});